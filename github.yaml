# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
# OBS: o Arquivo gerado pela firebase original precisa ser mudado, adcionando dentro de
#.gitbub/workflows/firebase-hosting-merge.yml as etapas da linha 15 ate 28 deste aquivo

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        # install node
        with:
          node-version: 18
      - name: Install dependencies
        run: npm install
      # install angular CLI
      - name: Install Angular CLI
        run: npm install -g @angular/cli
      # making the build
      #- uses: actions/checkout@v4
      - name: Build
        run: npm run build

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_EBOOK_FRONT_GRID_FLEX }}
          channelId: live
          projectId: ebook-front-grid-flex









